<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.patient">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Patient"
        android:usesCleartextTraffic="true">
        <activity android:name=".CurrentEquipActivity"></activity>
        <activity android:name=".ChangePassword" />
        <activity android:name=".EditProfile" />
        <activity android:name=".Myprofile" />
        <activity android:name=".MedicalStatus" />
        <activity android:name=".EquipmentDetails" />
        <activity android:name=".EquipmentActivity" />
        <activity android:name=".EquipmentHomeActvity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".Register2Activtiy" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>